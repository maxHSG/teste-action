name: 'Your name here'
description: 'Provide a description here'
author: 'Your name or organization here'
inputs:
  ssh-port:
    required: true
    description: 'Porta do ssh'
  ssh-username:
    required: true
    description: 'username do ssh'
  ssh-password:
    required: true
    description: 'password do ssh'
  key:
    required: true
    description: 'password do ssh'
runs:
  using: 'node16'
  main: 'dist/index.js'
